# Sample app for Minimal Api for .net 7

## How to create a bearer token
From the project catalog run following command:
`dotnet user-jwts create --role "manager" --claim "permission=can_edit_menu"`

